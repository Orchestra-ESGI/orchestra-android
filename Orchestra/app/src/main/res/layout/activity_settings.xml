<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:descendantFocusability="beforeDescendants"
    android:focusableInTouchMode="true"
    tools:context="view.ui.SettingsActivity">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/app_primary_blue"
        android:orientation="vertical"
        >

        <ImageView
            android:id="@+id/settings_profile_iv"
            android:layout_width="124dp"
            android:layout_height="124dp"
            android:layout_centerHorizontal="true"
            android:src="@drawable/ic_orchestra"/>

        <TextView
            android:id="@+id/settings_profile_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBottom="@id/settings_profile_iv"
            android:layout_centerHorizontal="true"
            android:textSize="24sp"
            android:textColor="@color/white"
            android:fontFamily="@font/gilroy_light"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/settings_rv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_below="@id/settings_profile_tv"
            android:layout_marginTop="20dp"
            android:layout_marginBottom="20dp"
            android:isScrollContainer="false"
            android:focusable="false"/>

        <TextView
            android:id="@+id/settings_version_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/settings_rv"
            android:layout_centerHorizontal="true"
            android:layout_alignParentBottom="true"
            android:text="v 1.0"
            android:paddingBottom="20dp"
            android:textSize="24sp"
            android:fontFamily="@font/gilroy_light"
            android:textColor="@color/white"/>

    </RelativeLayout>


</ScrollView>
